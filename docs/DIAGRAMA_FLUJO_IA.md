# 📊 Diagrama de Flujo - Asistente IA

## 🔄 Flujo Completo del Sistema

```
┌──────────────────────────────────────────────────────────────┐
│                    USUARIO EN NAVEGADOR                      │
│  (http://localhost/Ecommerce-Tinkuy/public/index.php)       │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ 1. Usuario escribe consulta
                         │    "chompa de alpaca"
                         ▼
┌──────────────────────────────────────────────────────────────┐
│              FORMULARIO HTML (index.php)                     │
│  ┌────────────────────────────────────────────────────┐     │
│  │  <form id="iaSearchForm">                          │     │
│  │    <input id="iaSearchInput" placeholder="...">    │     │
│  │    <button type="submit">🔍 Buscar</button>        │     │
│  │  </form>                                           │     │
│  └────────────────────────────────────────────────────┘     │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ 2. JavaScript captura submit
                         │    e.preventDefault()
                         ▼
┌──────────────────────────────────────────────────────────────┐
│              JAVASCRIPT FETCH (index.php)                    │
│                                                              │
│  fetch("/public/deepseek_search.php", {                     │
│    method: "POST",                                           │
│    headers: { "Content-Type": "application/json" },         │
│    body: JSON.stringify({ query: "chompa de alpaca" })      │
│  })                                                          │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ 3. POST Request
                         │    { "query": "chompa de alpaca" }
                         ▼
┌──────────────────────────────────────────────────────────────┐
│        PROXY PÚBLICO (public/deepseek_search.php)            │
│                                                              │
│  <?php                                                       │
│  define('BASE_PATH', dirname(__DIR__));                      │
│  require_once BASE_PATH . '/src/Views/misc/...';            │
│  ?>                                                          │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ 4. Include del archivo real
                         ▼
┌──────────────────────────────────────────────────────────────┐
│      ENDPOINT PRINCIPAL (src/Views/misc/deepseek_search.php) │
│                                                              │
│  ✅ Validación método POST                                   │
│  ✅ Validación query no vacío                                │
│  ✅ Preparar payload para OpenRouter                         │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ 5. cURL POST Request
                         │    https://openrouter.ai/api/v1/chat/completions
                         ▼
┌──────────────────────────────────────────────────────────────┐
│           OPENROUTER API (openrouter.ai)                     │
│                                                              │
│  Modelo: deepseek/deepseek-chat                              │
│  Prompt: "Eres un asistente de tienda de artesanías..."     │
│  Mensaje: "chompa de alpaca"                                 │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ 6. Respuesta IA
                         │    { "choices": [{ "message": { "content": ... }}] }
                         ▼
┌──────────────────────────────────────────────────────────────┐
│      PROCESAMIENTO (src/Views/misc/deepseek_search.php)      │
│                                                              │
│  ✅ Decodificar JSON                                         │
│  ✅ Limpiar markdown (```json ... ```)                       │
│  ✅ Extraer texto y keyword                                  │
│  ✅ Validar estructura                                       │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ 7. Respuesta JSON
                         │    {
                         │      "texto": "Nuestras chompas...",
                         │      "keyword": "chompa"
                         │    }
                         ▼
┌──────────────────────────────────────────────────────────────┐
│              JAVASCRIPT RESPONSE (index.php)                 │
│                                                              │
│  const data = await res.json();                              │
│  suggestionDiv.innerHTML = data.texto;                       │
│                                                              │
│  setTimeout(() => {                                          │
│    window.location.href = "?page=products&buscar=" +        │
│                           data.keyword;                      │
│  }, 10000);                                                  │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ 8. Mostrar recomendación (10s)
                         │    "Nuestras chompas de alpaca..."
                         ▼
┌──────────────────────────────────────────────────────────────┐
│                  USUARIO VE RECOMENDACIÓN                    │
│  ┌────────────────────────────────────────────────────┐     │
│  │  🤖 Recomendación IA:                              │     │
│  │  Nuestras chompas de alpaca son suaves y          │     │
│  │  abrigadoras, ideales para clima frío.            │     │
│  └────────────────────────────────────────────────────┘     │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ 9. Espera 10 segundos
                         │    (usuario puede leer)
                         ▼
┌──────────────────────────────────────────────────────────────┐
│              REDIRECCIÓN AUTOMÁTICA                          │
│                                                              │
│  window.location.href = "?page=products&buscar=chompa"      │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ 10. Nueva página
                         ▼
┌──────────────────────────────────────────────────────────────┐
│          CATÁLOGO FILTRADO (products.php)                    │
│                                                              │
│  Mostrando productos que coinciden con "chompa"              │
│  - Chompa de Alpaca Azul                                     │
│  - Chompa Tradicional Andina                                 │
│  - Chompa de Lana Premium                                    │
└──────────────────────────────────────────────────────────────┘
```

## 🔀 Flujo de Manejo de Errores

```
┌─────────────────────┐
│  Usuario envía      │
│  query vacío ""     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  deepseek_search.php valida         │
│  if (empty(trim($query)))           │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  HTTP 400 Bad Request               │
│  {                                  │
│    "error": "Query vacío..."        │
│  }                                  │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  JavaScript muestra error           │
│  "Por favor, escribe algo..."       │
└─────────────────────────────────────┘
```

```
┌─────────────────────┐
│  Usuario usa GET    │
│  en vez de POST     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  deepseek_search.php valida         │
│  if ($_SERVER['REQUEST_METHOD']     │
│      !== 'POST')                    │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  HTTP 405 Method Not Allowed        │
│  {                                  │
│    "error": "Método no permitido"   │
│  }                                  │
└─────────────────────────────────────┘
```

```
┌─────────────────────┐
│  API OpenRouter     │
│  no responde        │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  cURL timeout o error               │
│  $http_code !== 200                 │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  HTTP 500 Internal Error            │
│  {                                  │
│    "error": "Error al conectar...", │
│    "texto": "Lo sentimos...",       │
│    "keyword": ""                    │
│  }                                  │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  JavaScript muestra mensaje         │
│  "❌ Error con el asistente"        │
└─────────────────────────────────────┘
```

## 📦 Estructura de Datos

### Petición (Cliente → Servidor):
```json
{
  "query": "chompa de alpaca"
}
```

### Petición (Servidor → OpenRouter):
```json
{
  "model": "deepseek/deepseek-chat",
  "messages": [
    {
      "role": "system",
      "content": "Eres un asistente de tienda de artesanías peruanas..."
    },
    {
      "role": "user",
      "content": "chompa de alpaca"
    }
  ]
}
```

### Respuesta (OpenRouter → Servidor):
```json
{
  "id": "gen-xxxxx",
  "model": "deepseek/deepseek-chat",
  "choices": [
    {
      "message": {
        "role": "assistant",
        "content": "{\"texto\":\"Nuestras chompas de alpaca son suaves...\", \"keyword\":\"chompa\"}"
      }
    }
  ]
}
```

### Respuesta (Servidor → Cliente):
```json
{
  "texto": "Nuestras chompas de alpaca son suaves y abrigadoras, ideales para clima frío",
  "keyword": "chompa"
}
```

## 🎯 Puntos Clave de Integración

1. **Proxy Pattern**: `public/deepseek_search.php` → `src/Views/misc/deepseek_search.php`
   - Permite acceso público sin exponer estructura interna

2. **Validación en Capas**:
   - Capa 1: JavaScript (frontend) - valida input vacío
   - Capa 2: PHP (backend) - valida método POST y query
   - Capa 3: API (OpenRouter) - valida request structure

3. **Manejo de Errores Graceful**:
   - Siempre retorna JSON válido
   - HTTP status codes apropiados (400, 405, 500)
   - Mensajes descriptivos para debugging

4. **User Experience**:
   - Loading state: "🤖 Pensando..."
   - Success state: Muestra recomendación
   - Error state: Mensaje claro de error
   - Auto-redirect: Después de 10s para dar tiempo de leer

## 🔒 Seguridad Implementada

✅ Validación de método HTTP
✅ Validación de input vacío
✅ HTTP status codes correctos
✅ Manejo de excepciones
✅ Timeout en cURL
✅ JSON encode/decode con validación

## 📈 Métricas de Performance

- **Tiempo de respuesta IA**: ~500-1000ms
- **Timeout configurado**: 30s (default cURL)
- **Tamaño respuesta**: ~200-500 bytes
- **Redirección**: 10 segundos (configurable)

---

**Creado**: 15 de noviembre de 2025
**Versión**: 1.0.0
